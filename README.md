# xslang.org

Marketing site for the [XS programming language](https://github.com/xs-lang0/xs).
Next.js, deployed on Vercel.

```sh
npm install
npm run dev
```

Version pin in `src/lib/version.ts` follows the latest xs release.
